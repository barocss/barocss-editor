import { describe, it } from 'vitest';

// Extension implementations live in @barocss/extensions; tests for them run in that package.
// Skipped here to avoid editor-core depending on extensions (cycle).
describe.skip('Extensions', () => {
  it('placeholder', () => {});
});
