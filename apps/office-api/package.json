{
  "name": "@barocss/office-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @barocss/office-service build && tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "dev": "pnpm build && pnpm start",
    "test": "pnpm build && vitest run",
    "type-check": "tsc --noEmit -p tsconfig.json",
    "test:company:oidc": "pnpm build && node test/company-member.oidc.integration.mjs"
  },
  "dependencies": {
    "@barocss/office-service": "workspace:^",
    "fastify": "5.12.5",
    "jose": "6.2.12",
    "pg": "8.23.0"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/pg": "8.23.1"
  }
}
